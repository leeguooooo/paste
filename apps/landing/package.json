{
  "name": "@paste/landing",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "pages:dev": "wrangler pages dev ./dist",
    "pages:deploy": "npm run build && wrangler pages deploy ./dist --project-name paste-marketing --branch main --commit-dirty=true --commit-message \"deploy landing\"",
    "deploy": "npm run build && wrangler pages deploy ./dist --project-name paste-marketing --branch main --commit-dirty=true --commit-message \"deploy landing\""
  },
  "dependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "lucide-react": "^0.446.0",
    "framer-motion": "^11.11.1"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^4.3.3",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.14",
    "typescript": "^5.6.3",
    "vite": "^6.0.0",
    "wrangler": "^4.3.0"
  }
}
